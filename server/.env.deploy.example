# 部署連線設定範本。複製為 .env.deploy 並填入實值（.env.deploy 已在 .gitignore，不進版控）。
DEPLOY_HOST=your.vps.host
DEPLOY_USER=deploy-user
DEPLOY_SSH_KEY=~/.ssh/your_key
DEPLOY_DIR=/opt/purr-purr-town
DEPLOY_DB_USER=purrtown
DEPLOY_DB_NAME=purr_purr_town
